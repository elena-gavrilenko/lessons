import { TextContainer } from "./TextContainer"

export const Cell = (props) => {
    return (
        <td>
            {/* <TextContainer info={props.info}/> */}
            <TextContainer />
        </td>
    )
}