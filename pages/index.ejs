<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <!-- <title>Simple page</title> -->
    <title><%= title %></title>
</head>
<body>
    <% if (created) { %>
        <div class="alert alert-success">
            Note has been created
        </div>
    <% } %>
    <div class="container">
        <h1>This is working</h1>
        <form action="/" method="post" class="mb-2">
            <div class="form-group mb-2">
                <input type="text" required name="title" class="form-control">
            </div>
            <button class="btn btn-primary" type="submit">Create</button>
        </form>
        <% if (notes.length) { %>
            <div class="card">
                <ul class="list-group">
                    <% for (let i = 0; i < notes.length; i++) { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                           <span><%= notes[i].title %> </span>
                           <div>
                            <button class="btn btn-primary" data-type="edit"  data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>">Edit</button>
                            <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id %>">&times;</button>
                           </div>
                           
                        </li>
                    <% } %>
                </ul>
            </div>
        <% } else { %>
            <p>No notes!</p>
        <% } %>
    </div>
    <a href="<%= link %>" class="m-5">Login page</a>
    <script src="/app.js"></script>
</body>
</html>